services:
  wikialtas:
    image: hao0114/wikialtas:latest
    container_name: wikialtas
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # 数据持久化：SQLite 库、媒体库清单、会话与工单都在这里
      - ${DATA_DIR:-/mnt/Docker/WikiAltas}/data:/app/data
