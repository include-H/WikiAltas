name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache-dependency-path: backend/go.sum

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Embed frontend into backend
        run: |
          rm -rf backend/web/dist
          cp -r frontend/dist backend/web/dist

      - name: Download Go dependencies
        working-directory: backend
        run: go mod download

      - name: Build release package
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          OUT="release/wikiatlas-${VERSION}-linux-amd64"
          mkdir -p "$OUT/data"
          (cd backend && CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags '-s -w' -o "../${OUT}/wikiatlas" ./cmd/wikiatlas)
          cp -r skills "$OUT/skills"
          cp README.md "$OUT/README.md"

      - name: Package release artifacts
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          cd release
          tar -czf "wikiatlas-${VERSION}-linux-amd64.tar.gz" "wikiatlas-${VERSION}-linux-amd64"
          zip -r "wikiatlas-${VERSION}-linux-amd64.zip" "wikiatlas-${VERSION}-linux-amd64"
          sha256sum "wikiatlas-${VERSION}-linux-amd64.tar.gz" "wikiatlas-${VERSION}-linux-amd64.zip" > "wikiatlas-${VERSION}-checksums.txt"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            release/wikiatlas-${{ github.ref_name }}-linux-amd64.tar.gz
            release/wikiatlas-${{ github.ref_name }}-linux-amd64.zip
            release/wikiatlas-${{ github.ref_name }}-checksums.txt

  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: build-and-release
    environment: DOCKERHUB_TOKEN

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.2.0

      - name: Login to Docker Hub
        uses: docker/login-action@v4.6.0
        with:
          username: hao0114
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6.2.0
        with:
          images: hao0114/wikialtas
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v7.3.0
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
