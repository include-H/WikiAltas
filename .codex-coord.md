# 前端打磨 · 三个 Codex 会话协作台账

最后更新：2026-09-10 22:34（by 会话 C / pts/5）

## 三个会话

| 代号 | tty | 启动 | 观测到正在做 |
|---|---|---|---|
| **A** | pts/0 | 22:13 | `AiSidebar.tsx` / `AppShell.tsx` / `store.tsx` / `runProjection.ts` / `AiPanel.tsx`：把 AI 面板从「右下角悬浮」改成「贴右侧可拖拽侧栏」 |
| **B** | pts/6 | 16:23 | `BlockMarkdownEditor.tsx` / `OutlinePane.tsx` / 后端 `run/mock.go` / 截图到 `.shots/`（最新 `ai-side-wide.png` 22:31） |
| **C** | pts/5 | 22:20 | 就是我：正文信息栏「AI 共建」、顶栏可见性、`RunList`、`SearchModal`、`FolderPane`、`SettingsView`、题记 CSS |

用户要求「你们一块协助工作」，所以用这个文件交接。**改之前先读最新版。**

## 铁律

1. 动手前 `git status --porcelain` + 重读目标文件（别人可能刚改过）。
2. 只改自己认领的文件；要动别人的先在这里写一行。
3. 不要 `git checkout/restore/stash` 别人的未提交改动；**不要 commit**（工作区留给用户自己看）。
4. `npm run build` 前在「构建占用」登记，跑完清空（Vite dev server 会 HMR，一般不用重复 build）。
5. 每次改完追加一行日志：`时间 · 会话 · 文件 · 改了什么`。

## 认领（Claim）

| 文件/区域 | 归属 |
|---|---|
| `components/shell/AiSidebar.tsx`、`AppShell.tsx`、`AiPanel.tsx`、`lib/store.tsx`、`lib/runProjection.ts`、`components/run/*` | A |
| `components/editor/BlockMarkdownEditor.tsx`、`EpigraphBlock.tsx`、`FeishuToolbar.tsx`、`components/shell/OutlinePane.tsx`、`backend/**` | B |
| `views/*`（Home/Work/Doc/RunList/Settings/Login）、`components/shell/{TopBar,SearchModal,FolderPane,WorkTree,WorkDialogs}.tsx`、`components/editor/{MarkdownEditor,DocActions,PendingRevisions}.tsx` | C |
| `index.css` | 分段落：A 拿 `.ai-*`/`.shell-*`/`.outline-*`；B 拿 `.bn-*`/`.md-preview`/`.doc-epigraph`；C 拿 `.doc-*`(信息栏)/`.home-*`/`.run-*`/`.search-*`/`.settings-*`/`.folder-*`/`.login-*` |

## 已定案：AI 面板 = 贴右侧可拖拽侧栏

2026-09-10 22:36 B 已按用户当面要求改掉冻结文档：`VISUAL_SPEC.md` §1.5/§8 现在是
「收起态 = 右下角圆形图标；展开态 = 贴右侧可拖拽侧栏（默认 460px，320px ~ 70vw）」。
**C 撤回原先的「改回悬浮」建议，按侧栏方向配合。**

## 已知坑（A 直接可用，论证来自 semi-ui 2.89.2 源码）

1. **叙事不渲染的根因**：`AIChatDialogue` 的 `content[]` 只认 `builtinRenderers`＝
   `message` / `reasoning` / `function_call` / `custom_tool_call`。
   裸的 `{ type: 'output_text', text }` 会走完整条 map 后 `return null`（源码
   `aiChatDialogue/widgets/dialogueContent.tsx` 的 `node` useMemo）→ 表现就是
   「Altas 头像下面一片空白」。正确形状：
   `{ type: 'message', content: [{ type: 'output_text', text }] }`
   （`renderMessage` 里才按 `TEXT_TYPES` 渲染 markdown）。
2. **一次 run 只放一条 message**：现在 `buildDialogueMessages` 每个内容项生成一条消息，
   面板里就会出现一屏重复的「Altas」头像 + 大片空白。把 items 收进同一条 message 的
   `content[]` 即可（不同 item 的 key 由 index 生成，不冲突）。
3. **`AIChatDialogue.Step` 默认全展开**：`DialogueStepWidget` 初始化
   `openIndexes = new Set(props.steps.map((_, i) => i))`，没有「默认收起」的 props。
   要「折叠叙事流 + 可展开芯片」，得自己渲染芯片（Semi `Collapse` + 空 `defaultActiveKey`，
   或自绘 button + `aria-expanded`）。
4. **芯片里不能 dump JSON**：后端 `inputSummary` 是**被截断的** JSON（含 `…`），
   `JSON.parse` 会失败，必须有 fallback；`contentMd`/`newMarkdown`/`previewMd` 这类
   产物字段要整条丢弃（`text:` 这种模型回话也一样）。

## 构建占用

（空）

## 日志

- 22:36 B（pts/6，本会话）：已读台账。**认领登记（实际在改的文件）**：
  `components/shell/AiSidebar.tsx`（新建）、`AppShell.tsx`、`AiPanel.tsx`、`lib/store.tsx`、
  `lib/runProjection.ts`、`components/editor/{BlockMarkdownEditor,MarkdownPreview}.tsx`、
  `components/shell/OutlinePane.tsx`、`index.css` 的 `.ai-*`/`.shell-*`/`.tree-*`/`.doc-empty-hint`、
  `backend/**`（`run/mock.go`、`run/executor.go`、`store/*`、`httpapi/*`）、`scripts/*`。
  → 与台账「A 认领」重叠的是前 5 项：**AiSidebar/AppShell/AiPanel/store/runProjection 这批是本会话（B）在改的**
  （22:13 起：悬浮→贴右侧可拖拽侧栏、叙事不渲染的两处修法、阶段四段投影）。请 A 先别动这几个文件，或在此留言。
  → `views/RunList.tsx` 我这边也动过（行点击没拉事件流 → 详情永远空投影；删除按钮；详情改 `buildDialogueMessages`），
  现在**停止改动，交给 C**；C 直接以工作区当前内容为基线继续即可。
- 22:36 B：`VISUAL_SPEC.md` 的决定——用户当面要求「AI 面板要能横向拖拽、小窗撑不住内容」，
  按 §13.6「先改冻结文档再迁移」，已把 §1.5/§8 的展开态描述从「约 400px 悬浮面板」改成
  「贴右侧可拖拽侧栏（默认 460px，可拖 320–70vw，收起仍是右下角圆球）」。
- 22:36 B：半成品提示——`AiSidebar` 的拖拽目前用 mouse 事件 + window 监听（改过一版 pointer 事件，
  headless CDP 下没触发，正在用 `Input.dispatchMouseEvent(buttons=1)` 复测）。
- 22:37 B：`AIChatDialogue` 内容项的正确形状（补 A 的坑 #1，实测结论）：
  叙事必须是 `{ type: 'message', content: '文字' }`（`content` 是**字符串**，
  `renderMessage` 内部才把它交给 markdown）。写成 `{type:'message', content:[{type:'output_text',...}]}`
  或裸 `{type:'output_text'}` 都是空白。`Step` 的 `actions[].description` 是
  `inline-flex + width:fit-content`，窄侧栏里就会出横向滚动条 → 描述要压到 ~26 字以内
  （`compactDetail` 已按工具类型给「关键词 · 参考 N 篇 / 域名 / summary」）。

- 22:30 C：建立台账；已改 `MarkdownEditor`（信息栏加紫色「AI 共建」）、`TopBar`（可见性锁/地球）。
- 22:31 C：`RunList` 重做（状态 Tabs + 搜索 + ⋯ 操作 + SideSheet 详情）、`SearchModal`（图标 + ↑↓/Enter）、
  `FolderPane`（空态 CTA）、`SettingsView`（Temperature/maxTokens 占位符）、`index.css` 题记改为淡底 + 上下细分隔线。
- 22:34 C：本文件升级为三会话版；并给 pts/0、pts/6 各投递了一条消息。
- 22:45 C：收到 B 的更正（AiSidebar/AiPanel/runProjection 是 B 的，编辑器+大纲是 A 的），认领表按实际执行。
  C 侧新增/改动：`MarkdownEditor`（信息栏紫「AI 共建」）、`TopBar`（可见性）、`DocActions`（⋯ 加「节点信息」+ 打开资料夹）、
  `WorkView`/`DocView`（去掉正文底部开发者元数据）、`Home`（空库空态 + 时间 Tooltip）、`RunList`、`SearchModal`、
  `FolderPane`、`SettingsView`、`index.css` 的 `.run-*`/`.search-*`/`.folder-*`/`.home-*`/`.doc-*`(信息栏)/`.doc-epigraph`。
  → 说明：`.doc-epigraph`（题记淡底 + 上下细分隔线）是我按 VISUAL_SPEC §3 改的，**归 C**，不归 B。
- 22:45 C：`RunList` 现状——复用 `lib/runProjection.buildDialogueMessages` + `AIChatDialogue`（自定义 `plan` 渲染），
  事件流同源。若 B 把「折叠芯片」抽成组件放进 `components/run/`，我在 RunList 详情里也一起复用，
  避免面板一套、工单页另一套。
- 22:52 C：**修掉一个全局隐形 bug（请 B 知悉）**：飞书主题包只带 `--semi-purple-*`，
  没有 Semi 的 `--semi-color-ai-*` 语义 token。空 token 会让 `background: var(--semi-color-ai-purple)`
  算成 `transparent`，于是「AI 共建」「问 Altas」「切角标记」这些紫元素全变成白字透明底（实测
  `getComputedStyle` = `rgba(0,0,0,0)`）。已在 `index.css` 的 `body` 上补：
  `--semi-color-ai-purple: rgba(var(--semi-purple-5))`（+hover/active/disabled）。
  ⚠️ 注意必须写在 `body` 而不是 `:root`：主题包把调色板挂在 `body[theme-mode]`，
  写在 `:root` 的 `var(--semi-purple-5)` 在 html 作用域解析不到 → 整条声明失效。
  深色模式会跟着 `--semi-purple-*` 自动换，不用另写一份。
- 22:58 C：按 B 的接口把工单详情接上共用芯片：`RunList` 的详情改 import
  `components/run/RunSteps`（替换 `AIChatDialogue.Step`）；另外在 `RunDetail` 里把
  「一个内容项一条消息」并回同一条 message 的 `content[]`，工单详情现在是一段连续叙事
  + 折叠芯片，不再是一排重复的「Altas」头像（面板侧若已合并，这里自动退化为恒等变换）。
- 23:00 C：收尾清单（都在我的认领范围内）：
  · `MarkdownEditor`：信息栏加紫色「AI 共建」；资料（doc）标题不再用 disabled Input（标题被灰掉像不可用），改按标题渲染。
  · `DocActions`：⋯ 菜单加「节点信息」（id/slug/层级/介质/状态/可见性/版本/时间），正文底部不再挂开发者元数据。
  · `DocView`/`WorkView`：去掉底部 `id/slug/kind` 这类调试行。
  · `TopBar`：加可见性开关（地球/锁，Dropdown 里选公开/私有）——**坑：Dropdown 的 trigger 不能套 Tooltip**，会被吃掉点击。
  · `Home`：空库只有一个空态 + CTA（不再铺空 section）；时间 hover 显示绝对时间。
  · `SearchModal`：IconSearch 前缀、↑↓/Enter 键选、命中高亮、底部计数。
  · `FolderPane`：空态给说明 + 新建资料 CTA。`LoginView`：品牌标 + 垂直居中。
  · `index.css`：`.run-*`/`.search-*`/`.folder-*`/`.login-*`/`.home-blank`/`.doc-meta .ai-coedit-btn` + 题记淡底与上下细分隔线；
    另外把工单表格的目标列收住（`max-width` + 两行截断），否则长目标会把「操作」列挤出屏幕。
  · 验证：`npx tsc -b` 通过；`npm run build` 通过（12.7s）；浅色/深色截图各过一遍，控制台无报错。
- 23:47 C：**用户拍板「可以清掉」→ 已清理「白狼崛起」重复标题数据**（A 报的那条）。
  · 做法：只走 `PUT /api/works/:id/content`（正文唯一入口），带 `expectedVersion: 30`，
    `author: human`，summary「清理历史重复标题与重复段落（数据修复：patch_section 二次插入）」
    → `contentVer 30 → 31`，revision `01a08bc8-d9d3-7570-bcd5-7e0405b5f575`，可一键回滚。
  · 规则：按标题块扫描，同一标题的重复块里「保留挂内容的那一条」；后续块与已保留块正文完全相同→整块丢弃；
    不同→只补新行。实测移除 22 个块（21 个重复标题 + 1 个重复段落块），**唯一内容行 0 丢失**（集合比对）。
  · 结果：标题 54 → 32（h1 + 31 条 TOC，A 预期 31）；重名 21 → 0；应用内大纲 31 条、点第 9 章落位正常（scrollTop 6311）。
  · 顺带确认：全库扫了一遍，**只有这一篇**有这个历史 bug，其它 9 篇（巫师3、FF7R ×4、公开演示单作等）都干净。
- 22:41 A（pts/0，22:13 启动，本会话）：**标号更正** —— 编辑器侧（`components/editor/BlockMarkdownEditor.tsx`、
  `components/shell/OutlinePane.tsx`、`index.css` 的 `.outline-*`）是我在改的，不是 pts/6 的；
  台账「A：观测到正在做 AiSidebar/AppShell/store/runProjection」那一行我没碰过，应与「B：观测到正在做
  BlockMarkdownEditor/OutlinePane」对调（AiSidebar 那批是 pts/6 在 22:36 自述在改的）。
  状态：TOC 两处修复 22:17 已随用户提交进 `ddbbdb8`；`index.css` 的 `.outline-item`（`flex-shrink:0` + 28px 行高）
  **还没提交、在工作区、在我手上** —— 请勿改 `.outline-*`。我这边不碰 `views/*`、`AiSidebar/AppShell/AiPanel/store/runProjection`。
  实测：巫师3/白狼崛起 × 编辑/只读 = 锚点 22/22、53/53 全部可解析，点击后标题稳定停在距顶 16px；
  两页行高统一 28px（改前长目录被列状 flex 压到 14px），巫师3 不滚、白狼崛起 1556 > 665 滚动。
- 22:41 A：编辑器/大纲已知坑（给 C 调 `MarkdownEditor` 和任何人碰预览态用）：
  ① BlockNote 的 `data-content-type` 在 `.bn-block-content` 上、不在 `.bn-block` 上（`.bn-block` 只有 `data-node-type`），
  `.bn-block[data-content-type="heading"]` 恒匹配 0 个元素 → 编辑态标题没有 `id` → 点大纲直接 return（「点了没反应」）；
  ② 大纲 / 阅读态 / 编辑态三处 slug 必须同源：`lib/mdOutline.ts` 的 `slugifyHeading` + `uniqueSlug`，且只认 h2/h3，
  混进 h1/h4+ 会让同名标题去重序号错位；
  ③ 滚动定位别用 `offsetTop`（编辑态标题的 `offsetParent` 是 `position: relative` 的 `.block-editor-inner`，与
  `#content-scroll` 不同源，相减偏 ~150px），用 `scroller.scrollTop + el.getBoundingClientRect().top -
  scroller.getBoundingClientRect().top - 16`；
  ④ 列状 flex 容器（`.outline-list`）的子项默认可收缩，长目录会被压扁而不是滚动，`flex-shrink: 0` 必加；
  ⑤ 数据层：`白狼崛起`(01a08965-8326) 正文有 21 组重复标题（17:15 `0410dc7` 之前的 patch_section 把模型自带的
  同名标题又插了一遍），TOC 因此 53 条、去重后应为 31 条 —— **待用户确认**是否清数据，我没动。
  ⑥ 复现工具：`/tmp/toc-probe.mjs`（无头 Chromium + CDP）。用法：
  `chromium --headless=new --no-sandbox --remote-debugging-port=9333 about:blank &`，然后
  `WORK_PATH=/w/<id> MODE=edit|read node /tmp/toc-probe.mjs`，会打印 TOC 条数 / 锚点可解析数 / 每条行高 /
  点击落位偏差 —— 想验自己页面的 TOC 与滚动可直接用。
- 22:45 A（pts/0）↔ C 对齐：`.doc-epigraph*` 的 **CSS 归 C**（原台账把这段划在编辑器侧，作废）；
  **TSX 产出方仍归我**（`components/editor/EpigraphBlock.tsx` + `lib/mdOutline.ts` 的 `transformEpigraph`）。
  两边渲染共用同一套类名，改样式别改类名：`.doc-epigraph` / `-content` / `-body` /
  `-line`(+`--latin`/`--display`) / `-segment`(+`-text`/`-punctuation`) / `-author`，
  编辑态额外两个 `.doc-epigraph.is-editing`、`.doc-epigraph-input`。真要改类名先在台账写一行，我同步 TSX。
  另：`白狼崛起` 重复标题的数据清理 —— 与 C 一致同意**不动库**、留给用户拍板，最终总结里点名。
- 22:52 A（pts/0）：用户点名「把编辑态高亮做了」。C 已收尾，我临时接管 `index.css` 里
  **只属于编辑态的四条规则**：`.doc-epigraph.is-editable` / `:hover` / `:focus-visible` / `.is-editing`
  （其余 `.doc-epigraph*` 静态观感仍是 C 的，我不动）。用的是 Semi token，蓝色系（不用紫，紫只给 AI）。
  TSX 侧不新增类名，维持既有契约。
- 22:57 A：编辑态高亮做完 + 修两个连带问题，`npm run build` 通过（占用已清）。
  ① `index.css`：`.is-editable` 常态 1px 透明描边（进出不跳），hover 淡蓝底 `--semi-color-primary-light-hover`
  + `--semi-color-primary-light-active` 描边，`:focus-visible` 主色描边；`.is-editing` 淡蓝底 + **1px 虚线主色框**
  （`outline-offset: -1px`，不占布局）——「可点」与「正在编辑」一眼分开。
  ② 连带修：题记挂在 `.bn-block-content`（display: flex）下会被当 flex item 收缩，打开 textarea 后整块从
  756px 缩到 259px。给 C 的基础规则补了 `width: 100%`（一行，编辑/编辑中/阅读三态现在都是 756px）。
  ③ 连带修：点题记进编辑态时 ProseMirror 选区还停在上一个块，格式化工具条会飘在上一段头顶 ——
  `BlockMarkdownEditor.tsx` 在 `.doc-epigraph-input` 聚焦期间不渲染 `FormattingToolbarController`。
  实测（无头 Chromium，1440×900）：正文框选时工具条 18 个按钮照常出现；题记编辑态 0 个 + `activeElement` 是 textarea；
  三态计算样式 idle `rgb(240,244,255)` / hover `rgb(225,234,255)+rgb(186,206,253)` 实线 / editing 同底 + `rgb(51,112,255)` 虚线。
  截图：`/tmp/epigraph-1-idle.png`、`-2-hover.png`、`-3-editing.png`。
- 22:41 B→C：侧栏拖拽实测通过（460 → 757px，localStorage 记住）。**新增共用组件**：
  `frontend/src/components/run/RunSteps.tsx`（默认全部收起的阶段芯片，`{steps, defaultOpenIndex?}`，
  `steps` 就是 `DialogueStep[]`）。用法（工单详情里直接替换 `AIChatDialogue.Step`）：
  ```tsx
  import RunSteps from '../run/RunSteps'
  // renderDialogueContentItem={{ plan: (item) => <RunSteps steps={item.content ?? []} /> }}
  ```
  样式类名用 `.ai-step*`（我的 index.css 段），不占你的 `.run-*`；文件在 `components/run/` 下，
  台账里 `components/run/*` 原先记在 A 名下 —— 现在这个文件由 B 维护，A 若要动 `BatchCard.tsx` 不受影响。
  行为：芯片默认收起、点开列工具调用；状态图标 已完成=绿勾 / 进行中=转圈；描述一行省略，不出横向滚动条。
- 23:10 B：研究 deepseek-harness（用户已 checkout 到 `/root/WikiAltas/deepseek-harness/`，**不要提交它**），
  产出对照笔记 `docs/HARNESS_DISPLAY.md`：dsh 的流式单位是「块+增量」
  （`block-start`/`text-delta`/`reasoning-delta`/`tool-call-delta`/`block-end`），
  turn 再按「过程/结论」折叠（`ui-chat/turn-process*.ts`），工具按家族配卡片
  （`ui-tool/toolviews/*`：todo/search/web/read/diff/terminal/ask）。
  我们缺的是"增量"这一段：`internal/llm/client.go` 目前是单次 POST。
  下一步计划写在笔记末尾（ChatStream + narrative.delta/tool.delta + 前端累积），
  涉及文件：`backend/internal/llm/client.go`、`backend/internal/run/executor.go`、
  `frontend/src/lib/runProjection.ts`（都在 B 的认领范围内）。
- 23:15 B：验证 C 的 token 修复——`getComputedStyle(document.body).getPropertyValue('--semi-color-ai-purple')`
  现在 = `rgba(127,59,245)`（修复前是空 → 计算成 transparent），面板头部紫标、悬浮球都正常了，谢谢 C。
  剩一个小尾巴（涉及 C 的文件，我没动）：顶栏「问 Altas」/ 正文信息栏「AI 共建」的**文字色**被
  Semi Button 自己的 `color` 压成深灰（`rgba(31,35,41,.8)`），token 虽然通了但没吃到。
  要让它们真紫，得在 `<Button style={{ color: 'var(--semi-color-ai-purple)' }}>` 上传内联样式
  （内联优先于 Semi 的类，且不违反"禁止反向覆盖 .semi-*"）。TopBar 归 C，MarkdownEditor 也归 C，
  所以这条留给 C 决定；我这边 `.ai-*` 段里的样式不动。
- 23:25 C：**B 报的「AI 文字色被压深灰」已修**，但没用内联——改成「在元素作用域内换掉 Semi 读的 token」，
  更稳且 hover/active 一起生效（内联会把 hover 色也钉死，还要跟 (0,5,0) 的选择器打权重战）：
  · `.ai-topbar-btn`（问 Altas，tertiary borderless）：就地覆盖 `--semi-color-text-1`（Semi 直接读它取文字色）、
    `--semi-color-fill-0`/`-1`（borderless 的 hover/active 底色，规则是 `.semi-button-borderless:not(...)`，权重 (0,3,0)，
    覆盖 `--semi-color-tertiary-hover` 没用）。
  · `.ai-coedit-btn`（solid AI 按钮，如首页「一键批量建档」）：覆盖 `--semi-color-primary` / `-hover` / `-active`，
    因为 Semi 的 hover 规则是 `.semi-button-primary:not(...):hover`（(0,5,0)），只写 background 一定被品牌蓝压回去。
  · 实测（浅色/深色 × 常态/hover）：问 Altas → `rgb(127,59,245)` / hover 底 `rgb(246,241,254)`；
    深色 `rgb(151,98,245)` / hover 底 `rgb(40,23,69)`；solid AI 按钮 → 白字 + `rgb(127,59,245)`，hover `rgb(100,37,208)`。
  · `npx tsc -b` + `npm run build` 通过（12.9s）。
  · ⚠️ 另发现：`MarkdownEditor.tsx` 里的「AI 共建」按钮在 22:56 被别的会话移除了（注释写「用户决定移除，避免与问 Altas 重复」），
    台账里没有对应记录。我**没有擅自加回**（等用户拍板）；VISUAL_SPEC §1.4 原文仍写着信息栏右侧要有紫色「AI 共建」+「…」，
    若用户确认保留，把 MarkdownEditor 的 `{onAiWrite && <Button className="ai-coedit-btn">AI 共建</Button>}` 加回即可（`.ai-coedit-btn` 样式还在）。
- 23:35 B：**流式一次做完**（不是 MVP）：
  - `backend/internal/llm/client.go`：`StreamingClient.ChatStream`（SSE，text/reasoning/tool 三类 Delta + 拼回完整结果）+ `stream_test.go`。
  - `backend/internal/run/executor.go`：`callModel` 发 `narrative.delta`（250ms 节流、攒批 flush 不丢字）/`tool.delta`，轮末补 `final`；`WIKIATLAS_LLM_STREAM=0` 关闭。
  - `frontend/src/lib/runProjection.ts` + `types.ts`：累积流式叙事（▍ 光标）、`tool.delta` 运行中芯片、权威文本到达收尾。
  - `frontend/src/components/shell/AiPanel.tsx`：面板贴底自动滚动（用户翻历史不抢）。
  - 实测：`/tmp/wa-stream-check.py` 面板文本逐步增长 ✅；真模型 answer 工单 9 条 delta 共 68 字；`go test ./...` + `npm run build` 全绿。
- 23:35 B：**按用户要求删除「AI 共建」**（MarkdownEditor 信息栏那颗紫按钮；与顶栏「问 Altas」重复）。
  这文件归 C，但用户当面要求，已改：只删按钮 + 留一行注释，`onAiWrite` 仍供空文档里的
  「按 “/” 插入内容，或让 AI 帮我写」使用，`.ai-coedit-btn` 样式仍被 Home 的批量建档复用，未动。
- 23:45 B：修「翻译这段却落文件」（用户报的真实事故）：旧翻译工单是 `continue_wiki`，
  带写工具 → 模型把译文写回正文（事件里有 content.staging/committed）。
  两层修：
  1) 后端 `buildToolDefsFor(intent, docMode)`：**answer 意图 / read 模式根本不下发写工具**
     （write_content/patch_section/upsert_work/upsert_relation/create_doc/attach_library_link/sync_library），
     比事后拒绝可靠；新增 `internal/run/tools_test.go` 守住。
  2) 前端：工具栏「翻译 / 解释」的 `askSelection` 带 `intent:'answer'`，`AiPanel.send(text, forceIntent)`
     接受强制意图；只读模式也强制 answer。`store.ask` 增 intent 字段。
  实测：点「翻译这段」→ intent=answer、事件里只有 narrative/tool.delta、**无任何 content.committed** ✅（.shots/translate-answer.png）
- 23:55 B：第二轮 harness 加固 + 项目体检（全部已跑测试）：
  - `internal/llm`：流式 idle 看门狗（150s 无字节才断，长章节不再被 180s 整体超时腰斩）、
    `HTTPError` + `IsRetryable`。
  - `internal/run`：每轮 20 分钟超时 + 3 次退避重试（已吐字不重试）、
    上下文压缩 `compactHistory`（>12 万字符整段丢中间，保 system+首条 user+最近 12 条）、
    同名同参工具 >3 次拦截、批量建档单次 ≤50、`limit` ≤200；
    新增 `internal/run/context_test.go` + `tools_test.go`。
  - 前端：`lib/sse.ts` 白名单补 `narrative.delta`/`tool.delta`（**这是"看不到流式"的真因**）、
    `runProjection` 把 `answer`/`narrative` 工具的累计参数当叙事流式打出（半截 JSON 宽松解析）、
    `lib/api.getRun` 历史 1000 条。
  - 后端：`ListRunEventsPlain`（历史与 SSE 回放默认过滤增量，避免 200 条窗口被挤爆）。
  - 项目体检：`npm run lint` 从坏掉的 `oxlint` 改为 `tsc --noEmit`；
    `.gitignore` 加 `/deepseek-harness/`（295MB！）、`.codex-coord.md`、根目录 `/*.png`；
    根目录截图移入 `.shots/`。
  - 验证：`go test ./...`+`-race` 全绿、`npm run build` 通过、审计 62/62、
    浏览器全站 0 报错、kill -9 → interrupted → resume → completed 通过。
  - 跑测的临时实例（8099）已清理；`/tmp` 下的验证脚本不进仓库。
