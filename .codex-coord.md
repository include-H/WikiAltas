
- 09-11 16:2x（Claude 主会话，续作）：**面板展示改成「顺流时间线」**（对齐 MiMo / dsh）+ 三处修复。
  · 后端 `executor.go`：模型口吻的叙事统一走 `emitNarrative`，**连续同文只发一条**——
    修"同一句说两边"（narrative 工具与正文复述双发）。注意：主环境 8080 仍是旧二进制，重启后才生效。
  · 前端 `runProjection.ts` 重写：删掉四段相位分桶（芯片乱跳的根源），改成 事件顺序 = 展示顺序 的时间线：
    叙事句 → 工具组芯片（「已读取 N 处 · 已检索 N 次」，点开逐条明细+产物）→ 叙事句 …；
    计划块钉在时间线开头（只更新状态不挪位，跑完且零完成度则不显示）；头部「已处理 Xm Xs」。
    新组件 `components/run/TimelineItems.tsx`（ToolGroupChip / PlanBlock / ElapsedLine），
    `RunSteps.tsx` 已删（面板与工单页共用新组件；工单页去掉了"并回一条消息"的旧 hack）。
  · 会话视图「我说的话哪去了」：当前工单的提问从 `run.goal` 派生渲染成用户气泡（刷新/召回后也在）；
    「收到工单：…」系统回声不再展示。
  · 验证：tsc + build + go test 全绿；无头浏览器实测召回视图（截图 `.shots/ai-timeline-recall.png`、
    FF 那一单 `.shots/ai-timeline-ff.png`）。

- 09-11 18:xx（Claude 主会话，续作）：**双协议 LLM 客户端 + 思考（reasoning）全链路 + 时间线打磨**。
  · `internal/llm/anthropic.go`：Anthropic Messages 协议客户端（`/v1/messages`，x-api-key；
    system 顶层、tool_calls↔tool_use、连续 tool 结果并成一条 user、thinking.budget_tokens 映射：
    低1024/中4096/高16384/超高32768）+ 单元测试；`llm.NewClient(cfg)` 按 `cfg.Protocol` 选路。
  · 设置：`Settings.LLM.Protocol`（openai|anthropic）+ `ReasoningEffort`；设置页新增「协议 / 思考等级」；
    聊天框加飞书式「推理强度」菜单（自动/低/中/高/超高，showTick；自动=不指定走设置）；`CreateRunBody.reasoningEffort` 按单覆盖。
  · 思考链路：客户端同时认 `reasoning`（whatshao 网关）与 `reasoning_content`（OpenAI/DS 惯例）两个字段；
    执行器发 `reasoning.delta`（250ms 攒批） + 轮末权威 `reasoning`（落库，历史可回放；增量的历史过滤加 reasoning.delta）；
    前端 `ReasoningRow`：一行「思考 + 摘要」（进行中显示最后一句），展开全文。
  · system prompt **改写为英文**（用户决定：英文指令遵守度更高），末尾加中英双语「思考必须用简体中文」硬性要求；
    实测 DS 已输出中文思考。时间线：answer/narrative 工具不再出芯片（修"已作答 text:…"丑行）；流式叙事与上一句同文时收掉（修答案说两遍）。
  · 新建节点弹窗：选「系列/单作」时出现「所属宇宙 / 系列」选择器（修首页建节点挂在根上的孤儿问题）。
  · 测试环境（5174/8090）已切到 **DeepSeek Anthropic 端点**（deepseek-flash，测试库配置；主库未动）；主环境 8080 仍是旧二进制，重启后本批才生效。

- 09-11 19:xx（Claude 主会话，续作）：**AI 展示全面转 Semi 原生**（用户要求：先读全 Semi AI 组件再动手）。
  · 读完 AIChatDialogue（含 ContentItem 全类型：reasoning/function_call/web_search_call/annotations…）与 AIChatInput（含配置区域 Configure）。
  · 档位选择器改用原生 `renderConfigureArea` + `Configure.Select`（输入框左下「自动 ⌄」，即飞书同款位置）；
    工具行改用内置 `function_call` 渲染器（name + 人话化 arguments）；思考用内置 `reasoning`（「正在思考中…/已思考完成」）；
    移除「✓ 已完成」「已处理 Xs」两行（飞书没有，用户拍板）；手搓的 ToolGroupChip/ReasoningRow/ElapsedLine（TimelineItems.tsx）与对应 CSS 全部删除。
  · 「答案说两遍」三段根因全部收口：后端连续同文去重 + 前端流式同文收尾 + 轮末清 answer 流式槽（末段是主凶）。
  · 实测：search_web 行按原生样式渲染、思考行中文（英文 system prompt + 末尾中文思考硬性要求生效）、答案无重复；截图 `.shots/ai-native-rows.png`。

- 09-11 20:xx（Claude 主会话，续作）：**任务系统按 Claude Code `TodoWrite` 契约重做**（GitHub 调研后落地）。
  · 调研：拿到 Claude Code TodoWrite 工具描述全文（Piebald-AI/claude-code-system-prompts）与 dsh anchorSeq 思路。
    结论：之前模型"不建单"不是模型不乐意，而是旧 update_plan 的描述只有一句话、没有使用纪律。
  · 后端：新增 `todo_write` 工具，描述照 TodoWrite 契约改写（何时必须用/何时不用/恰好一个 in_progress/完成立刻标/卡住保留 in_progress/拿不准就用/任务名中文）；
    事件 `tasks.updated`（工具参数即状态，不落库）；control 工具（不出芯片）；系统提示加「Task list」纪律段 + 「答案只交一次」4b 条。
  · 前端：`tasks.updated` 进事件流；投影有任务时按「当前 in_progress 任务」分组——每个任务一条折叠条（标题=任务名，内容=该任务期间的思考+工具行，
    半成品 plan 折叠条之前那版正好接上）；无任务的旧工单退回聚合标题；`PlanSteps` 用 AIChatDialogue.Step 渲染（任务名+状态图标+展开分步）。
  · 实测（deepseek-flash）："分三个任务核实…" → 模型建了 5 个中文任务并三次实时更新状态（探测 completed → 核实 in_progress → 汇总 in_progress）；
    折叠条渲染出任务名 + 思考块 + 「已联网检索 · 参考 N 篇」行。截图 `.shots/ai-task-plan2.png`、`ai-task-fold.png`。
  · 主环境 8080 仍是旧二进制（这一大批含 todo_write 未生效）。

- 09-11 21:xx（Claude 主会话，续作）：**工具产物卡片化**（对齐飞书 SKILL.md 卡片与检索结果列表）。
  · 读了 semi-ui 源码：AIChatDialogue 内置渲染器只有 message/reasoning/function_call；代码卡片组件
    官方暴露为 `AIChatDialogue.defaultComponents.code`（标题栏+语言标签+复制键）——直接复用，不手搓。
  · 折叠条分步行：检索类产物（search_web）→ 编号来源列表（标题可点，对齐飞书）；读取/抓取类产物 → 官方代码卡片；
    read_skill 行改为具体文件名「已读取 技能中的 core.md」。检索类不再直接铺 URL 原文。
  · 验证：面板 9 个来源列表 + 思考块渲染正常；截图 `.shots/ai-sources-list.png`、`.shots/ai-artifact-card.png`。

- 09-11 24:xx（Claude 主会话，续作）：**首层折叠落地（MiMo 式）+ 投影定稿**。
  · GitHub 调研（OpenHands/Cline/Codex/Suna）结论：任务与过程折叠**两层不混**——折叠条标题一律用行为摘要+计数，
    任务清单独立渲染（Cline 一行 N/M）；Codex update_plan 注释原文"inputs are useful to clients"。OpenHands 的 double-text 修法与我们一致。
  · 投影定稿：整轮过程折进一条**首层折叠**「已处理 Xm Xs」（跑着展开+秒针跳、完成自动收起）；
    内含叙事句 + 过程折叠条（标题=「思考 · 已检索 N 次」聚合计数，展开=逐条工具行/来源列表/文件卡片）+ 任务进度行「任务 N/M · 正在：xxx」；
    **最终结论留在折叠外**。任务名不再参与命名（错位根因）。
  · 组件：PlanSteps.tsx 内 TurnFold（首层折叠）+ PlanSteps（过程折叠）+ TaskLine（任务行），叙事用 Semi MarkdownRender。
  · 截图 `.shots/ai-turn-fold.png`（11 条折叠全部收起、展开条层级正确、结论在外）。

- 09-12 00:xx（Claude 主会话，收尾）：**对齐 MiMo/Codex 的最终投影定稿**（用户提供 MiMo 自述 + Codex 源码 /codex/，已 gitignore）。
  · MiMo 自述核心：折叠条=UI 从引擎 part 事件本地聚合（计数=工具调用次数、按工具名分桶）；「已处理」=消息级 elapsed；
    diff=引擎写进 metadata.filediff；任务树是 SQLite 状态机、与折叠条不同层；part.updated=全量快照落库 + part.delta=文本增量不落库。
  · Codex：update_plan 纯为客户端渲染（"inputs are useful to clients"）；提示词金句"不要复读计划内容，harness 已显示"；
    回合收尾元数据「Worked for」仅 >60s 显示；工具行 • 动词 目标 + 状态圆点。
  · 落地：①写工具行 diff 统计（后端 LCS 算 additions/deletions → tool.done 载荷 → 行上 +N 绿/−M 红，实测 patch_section +2）
    ②工具行状态（进行中转圈/失败红）③首层折叠「已处理 Xm Xs」：短回合不显时长、中断显「已停止」④任务行 N/M 独立显示
    ⑤系统提示补 Codex 原文纪律（不复读任务清单）⑥会话/投影结构：turn 折叠 > burst 折叠（聚合计数标题）> 思考块/工具行/产物卡片，叙事与折叠按事件顺序交错。
  · 截图 `.shots/ai-diffstat2.png`（两级展开 + +2 diffstat）、`ai-turn-fold.png`。

- 09-12 12:xx（Claude 主会话）：**展示定稿（用户拍板：删掉回合耗时行与大折叠）**。
  · 「已处理 Xs」前后折腾数轮（折叠→底部计时→移除→状态行→再移除），最终按用户「处理不好就删掉」**整行删除**；
    同时大折叠容器（turn fold）也一并取消——叙事句、过程折叠条、最终输出**全部平铺在主线程**（最终正文 = 主会话，不被折叠吞掉）。
  · 运行态表达改为：输入框停止键 + 进行中行转圈 + 跑着的折叠条自动展开。
  · 配色打磨：Altas 头像 AI 紫、折叠体内 markdown 尺寸收束（h1-h4 压到 14px）、思考块缩进 22px 对齐、来源列表序号/链接色阶、运行中转圈。
  · 验证：turnFolds=0、最终报告各段为主线程可见消息；截图 `.shots/ai-flat.png`、`ai-color-before/after.png`。
